/**
 * 🧪 Google Sheets Service テスト（簡易版）
 */

import { describe, expect, it } from 'vitest';

describe('GoogleSheetsService', () => {
  it('基本テストが通ること', () => {
    expect(1 + 1).toBe(2);
  });

  it('文字列のテストが通ること', () => {
    expect('hello').toBe('hello');
  });
});
